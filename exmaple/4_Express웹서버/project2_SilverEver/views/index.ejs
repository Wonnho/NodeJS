<!DOCTYPE html>
<html>
<head>
    <style>
        .head {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 70%;
            margin: 0 auto;
            padding-bottom: 20px;
        }
        .button {
            background-color: #6694a6;
            border: none;
            color: white;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            transition-duration: 0.4s;
            cursor: pointer;
            padding: 10px 24px;
            border-radius: 12px;

        }

        .button:hover {
            background-color: #45a049;
        }

        table {
            border-collapse: collapse;
           width: 70%;
            border-radius: 5px;
            margin: 0 auto;
        }


        /* 홀수행에 대한 스타일 */
        tbody tr:nth-child(odd) {
            background-color: #cceeff;
        }

        /* 짝수행에 대한 스타일 */
        tbody tr:nth-child(even) {
            background-color: #ffffff;
        }
        td {
            text-align: center;
            padding: 8px;
            border-bottom: 1px solid #ddd;
        }

        th {
            font-family: sans-serif;
            background-color: #f2f2f2;
            font-size: 24px;
            text-align: center;
            padding: 9px;
        }

        h2 {
            text-align: center;
            font-size: 34px;
            }

        .paging {
            text-align: center;
            margin-top: 20px;
            font-size: 18px;
        }
    </style>
</head>
<body>
<div class="head">
<h2>게시판</h2>
<a href="/write" class="button">글쓰기</a>
</div>
<table>
    <thead>
    <tr>
        <th>제목</th>
        <th>글쓴이</th>
        <th>등록일</th>
        <th>조회수</th>
    </tr>
    </thead>
<tbody>
        <% for (let k=0;k<bulletin.length;k++){ %>
            <tr>

                <td><a href="/bulletinDetail/<%= bulletin[k][0] %>" class="post-link"><%= bulletin[k][3] %>[<%= bulletin[k][5] %>]</a></td>
                <td><%= bulletin[k][1] %> </td>
                <td><%= bulletin[k][4] %></td>
                <td><%= bulletin[k][5] %></td>
            </tr>
        <% } %>
</tbody>
</table>
<script>

</script>
<div class="paging">
    <% if (currentPage > 1) { %>
        <a href="/?page=<%= currentPage - 1 %>"><앞으로></a>
    <% } %>

    <% for (let k = startPage; k <= endPage; k++) { %>
        <% if (k === currentPage) { %>
            <span class="current-page"><%= k %></span>
        <% } else { %>
            <a href="/?page=<%= k %>"><%= k %></a>
        <% } %>
    <% } %>

    <!-- 현재 페이지에서 한 화면에 처리하는 페이지 갯수 보다 하나라도 더 많으면 다음 링크로 이동할 수 있는 조건   -->
    <% if ( (totalPage - currentPage +1) > maxPageNumber ) { %>
        <a href="/?page=<%= currentPage + 1 %>"><뒤로></a>
    <% } %>
</div>

</body>
</html>
